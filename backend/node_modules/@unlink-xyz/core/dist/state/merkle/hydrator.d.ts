export type BatchLeafLoader = (chainId: number) => Promise<{
    commitment: string;
    index: number;
}[]>;
type TreesInterface = {
    reset(chainId: number): void;
    addLeaf(chainId: number, value: bigint): {
        index: number;
    };
    getLeafCount?(chainId: number): number;
};
export declare function rebuildTreeFromStore({ chainId, trees, loadLeaves, }: {
    chainId: number;
    trees: TreesInterface;
    loadLeaves: BatchLeafLoader;
}): Promise<number>;
export type HydrateChainParams = {
    chainId: number;
    trees: TreesInterface;
    loadLeaves: BatchLeafLoader;
    hydrated: Set<number>;
};
/**
 * Hydrate the in-memory merkle tree for a chain, skipping if already done.
 * Call `hydrated.delete(chainId)` to invalidate and force a rebuild.
 */
export declare function hydrateChain({ chainId, trees, loadLeaves, hydrated, }: HydrateChainParams): Promise<number>;
export {};
//# sourceMappingURL=hydrator.d.ts.map